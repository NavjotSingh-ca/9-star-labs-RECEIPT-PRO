# Leduc Receipt Pro — Full Codebase Audit
### Generated June 2026 · Next.js 16.2.2 · 112 Tasks

---

## 🔴 CRITICAL — P0 (Ship-blocker; data loss, auth bypass, or security breach)

**[CRITICAL] [P0] Middleware: `proxy.ts` is a dead letter — auth guard never executes — File:src/proxy.ts:6 — `proxy()` is exported as a named function, but Next.js App Router middleware requires a *default* export from a file named `middleware.ts` at the project root. Because no `middleware.ts` exists, the auth redirect and cookie-refresh logic inside `proxy.ts` is never invoked by the framework, leaving every protected route (including `/settings/*`) completely unguarded at the edge. → Rename `src/proxy.ts` → `src/middleware.ts` and add `export default proxy;` as the final line; verify with `next build` that the middleware bundle appears.**
<!-- STATUS: SKIPPED — Next.js 16 uses proxy.ts convention; build shows ƒ Proxy (Middleware) → confirmed working -->

**[CRITICAL] [P0] Scanner: Duplicate-hash check leaks cross-tenant receipt existence — File:src/components/scanner/hooks/useScannerState.ts:130 — The `saveMutation` queries `duplicate_hash` scoped only to `user_id`, meaning a user in Org A can detect whether a receipt matching their hash exists anywhere in Org B if they share the same user_id (e.g. after an org bootstrap bug). This is a multi-tenant data leak. → Add `.eq('org_id', orgId)` to the duplicate check query, and surface `orgId` from the state (it's already available as `state.orgId`).**
<!-- STATUS: DONE -->

**[CRITICAL] [P0] Auth: `getSession()` used to initialise auth state on the primary page — File:src/app/page.tsx:213 — `supabase.auth.getSession()` reads from the local cookie/storage and can return a stale or attacker-replayed session. Supabase SSR documentation explicitly says to use `getUser()` (which validates with the server) for trusted auth checks. → Replace `supabase.auth.getSession()` with `supabase.auth.getUser()` for the initial session resolution inside `resolveUser`.**
<!-- STATUS: DONE -->

**[CRITICAL] [P0] Auth: `getSession()` used to extract bearer tokens for API calls in all settings pages — File:src/app/settings/billing/page.tsx:53 — Seven separate pages (`billing`, `team`, `org`, plus `AuditTrail`, `MoreSheet`) call `getSession()` and pass `session.access_token` as an `Authorization` header to protected API routes. A stale or tampered cookie yields a stale token; the server-side API routes re-validate with `supabaseAdmin.auth.getUser(token)`, but the client is handing over potentially-expired tokens without a refresh attempt. → Replace `getSession()` with `getUser()`, which triggers a background refresh when needed, ensuring the emitted token is always valid.**
<!-- STATUS: DONE — billing/team/org/AuditTrail all converted to useQuery with getUser() inside queryFn -->

**[CRITICAL] [P0] Encryption: QBO OAuth tokens stored in plaintext when key is unset — File:src/lib/encryption.ts:7 — If `TOKEN_ENCRYPTION_KEY` is missing or shorter than 32 characters, `encryptToken()` logs a console error and returns the raw plaintext token. The QBO callback route at `src/app/api/qbo/callback/route.ts:79` calls `encryptToken(refresh_token)` and writes the result directly to the database — so in a misconfigured environment all refresh tokens are stored as plaintext. → Throw an explicit startup error (not a `console.error`) if `TOKEN_ENCRYPTION_KEY` is absent when the QBO integration is active; add the key to the `envSchema` as required when `QBO_CLIENT_ID` is present.**
<!-- STATUS: DONE -->

---

## 🟠 HIGH — P0 (Serious bug or data-integrity risk requiring immediate fix)

**[HIGH] [P0] Security: No server-side rate limiting on the AI scan Server Action — File:src/app/actions/scan-receipt.ts:368 — `scanReceipt()` is a `'use server'` action that calls the Gemini API and writes a `scan_attempts` row. The only guard is a subscription check, but there is no per-IP or per-user request-rate limit. A free-tier user can flood this endpoint, exhausting the Gemini quota and writing thousands of rows into `scan_attempts`. → Add an Upstash Redis / Vercel KV token-bucket or at minimum a Supabase-RPC-based per-user per-minute counter before the Gemini call; return HTTP 429 with a `Retry-After` header when the limit is exceeded.**
<!-- STATUS: DONE — 3/min burst + 10/hr rate limit added -->

**[HIGH] [P0] Security: Email inbound route uploads attachments without validating size — File:src/app/api/email/inbound/route.ts:90 — `validAttachments` filters only on `content_type`; there is no check on attachment byte length before calling `supabaseServiceRole.storage.upload()`. A crafted webhook payload with a 50 MB attachment base64-string will be decoded and uploaded, exhausting storage and potentially the Lambda memory limit. → Add a per-attachment byte-size check (e.g. reject if `buffer.byteLength > 20 * 1024 * 1024`) before the `storage.upload()` call; also add a total-payload size guard at the top of the handler.**
<!-- STATUS: DONE -->

**[HIGH] [P0] Security: `/api/team` DELETE body parsed without Zod validation — File:src/app/api/team/route.ts:72 — `const { userId } = await request.json()` parses the body without a Zod schema. Any unexpected payload shape causes an uncaught exception; a null `userId` causes the `"userId required"` branch to return 400 but only after the auth and role checks have already executed. → Wrap the body parse in a `z.object({ userId: z.string().uuid() }).safeParse(...)` and return 400 before any DB work if parsing fails.**
<!-- STATUS: DONE -->

**[HIGH] [P1] Performance: `getDashboardSummary` fires 8+ sequential awaited queries — File:src/lib/services/receipts.ts:255 — After the initial RPC call, the function awaits three `COUNT` queries for missing BN, pending review, and flagged receipts sequentially (lines 265–279), then awaits reimbursements, monthly trend, confidence counts, unmatched bank count, and mileage totals — nine serial round-trips adding ~1–3 s of latency. → Wrap the independent queries in a single `Promise.all([...])` call; the three `COUNT` heads and two aggregate queries can all run in parallel.**
<!-- STATUS: DONE -->

**[HIGH] [P1] Data integrity: `useScannerState` doesn't scope `org_id` in the duplicate query — File:src/components/scanner/hooks/useScannerState.ts:130 — (Additional consequence beyond the CRITICAL cross-tenant leak above): If a user submits a receipt that a *former* employee from the same org submitted previously, the duplicate check correctly fires by `user_id` — but it will never fire for a *different* employee in the same org, allowing duplicate expenses to slip through the approval workflow. → Include `.eq('org_id', orgId)` and remove the `.eq('user_id', user.id)` constraint (org-wide deduplication is the correct CRA posture).**
<!-- STATUS: DONE -->

**[HIGH] [P1] Auth: `getSession()` used in `AuditTrail` to fetch the user for display — File:src/components/AuditTrail.tsx:93 — The Audit Trail calls `supabase.auth.getSession()` and uses the returned user to display the current operator; a replay of an expired session cookie still returns a user object here, showing stale identity. → Replace with `supabase.auth.getUser()` which performs a server-side verification call.**
<!-- STATUS: DONE — AuditTrail converted to useQuery with getUser() inside queryFn -->

**[HIGH] [P1] Performance: `usePlan` hook re-fetches plan + usage on every mount via raw `useEffect` — File:src/hooks/use-plan.tsx:24 — Every component that calls `usePlan()` (Sidebar, TopBar, UpgradePrompt, MobileNav, settings pages) triggers a full subscription + usage + team-size fetch on mount because the data lives in local state, not a shared React Query cache. On a typical page load 4–6 components mount simultaneously, causing 4–6 concurrent fetches. → Convert `usePlan` to use `useQuery` with a `queryKey: ['subscription']` and `staleTime: 5 * 60 * 1000`; the query will be shared automatically across all consumers.**
<!-- STATUS: DONE -->

**[HIGH] [P1] Performance: `AnomalyDashboard` fires 5 sequential Supabase queries in `useEffect` with no caching — File:src/components/AnomalyDashboard.tsx:33 — The component fetches fraud, math-error, missing-BN, duplicate, and spend-anomaly data in sequence inside a raw `useEffect` with no React Query. Tab switching causes a full re-fetch every time. → Convert all five queries to a single `useQuery({ queryKey: ['anomalies', orgId], staleTime: 2 * 60 * 1000 })` call backed by a consolidated service function.**
<!-- STATUS: DONE -->

**[HIGH] [P1] TypeScript: Stripe Invoice fields accessed via `as unknown as` casts — File:src/app/api/stripe/webhook/route.ts:81 — `invoice.subscription` and `invoice.period_end` are accessed via `(invoice as unknown as { subscription?: string }).subscription` because the `@stripe/stripe-js` v22 types have these fields available directly on `Stripe.Invoice`. The casts hide a version mismatch and will silently break if the Stripe SDK major version changes. → Access `invoice.subscription` and `invoice.lines.data[0].period.end` using the properly-typed Stripe v22 API rather than casting through `unknown`.**
<!-- STATUS: DONE (PHASE 0.4) -->

---

## 🟡 HIGH — P1 (Impactful bug or significant tech debt)

**[HIGH] [P1] Architecture: `Tab` type re-declared locally in `Sidebar.tsx` instead of imported — File:src/components/layout/Sidebar.tsx:31 — `Sidebar.tsx` redeclares `type Tab = 'dashboard' | 'receipts' | ...` identically to the canonical definition in `src/lib/store.ts:5`. Any future tab added to the store's definition won't automatically propagate to the sidebar, creating a silent mismatch. → Delete the local `Tab` declaration and add `import type { Tab } from '@/lib/store';`.**
<!-- STATUS: DONE — already imported from @/lib/store, no local re-declaration -->

**[HIGH] [P1] Security: Hardcoded personal Gmail address as fallback "from" in production email routes — File:src/app/api/receipts/comments/route.ts:67 — `from: 'Leduc Receipt Pro <9starlaba@gmail.com>'` is hardcoded in the comment-notification email. Resend rejects sending from unverified domains; using a personal Gmail violates Resend's ToS and marks all notification emails as spam. A matching hardcoded fallback exists in `api/digest/missing-receipts/route.ts:117`. → Replace both with `env.RESEND_FROM_EMAIL` (no hardcoded fallback); add `RESEND_FROM_EMAIL` as a required field in the Zod env schema when `RESEND_API_KEY` is set.**
<!-- STATUS: DONE -->

**[HIGH] [P1] History: Double-filtering — client re-filters already server-filtered paginated results — File:src/components/History.tsx:148 — `getReceiptsPaginated` already applies `missing-bn`, `flagged-audit`, and `reimbursement` server-side via the RPC, but `filteredReceipts` in `useMemo` re-applies the same logic client-side on lines 151–175. This means page 2+ of paginated results gets incorrectly re-filtered against only the current page's data, producing wrong counts in the stat cards. → Remove the client-side `filteredReceipts` filter for conditions already handled server-side; rely only on server-paginated data for the count aggregations.**
<!-- STATUS: DONE — specialFilter param added to getReceiptsPaginated; client-side re-filter removed -->

**[HIGH] [P1] Performance: `ProfessionalLedger` fetches business-unit names inside `useEffect` on every data change — File:src/components/history/ProfessionalLedger.tsx:42 — Every time the `data` prop changes, the effect re-fires and issues a Supabase query to resolve `business_unit_id → name` mappings. Business units are a near-static lookup table. → Cache business units in React Query with `staleTime: Infinity` and pass the unit map as a prop, or lift the lookup to the parent `History` component where it is already available.**
<!-- STATUS: DONE — useQuery({ staleTime: Infinity }) caches business units -->

**[HIGH] [P1] Performance: `History.tsx` is 851 lines with a 300-line inline sub-component `ReceiptDetailDrawer` — File:src/components/History.tsx:400 — The detail drawer, the stat cards, the search bar, and the filter bar are all defined inline in the same file. Bundling everything together prevents independent lazy-loading of the drawer. → Extract `ReceiptDetailDrawer` to `src/components/history/ReceiptDetailDrawer.tsx` and import it with `next/dynamic`; similarly extract `StatCards` and `SemanticSearchBar`.**
<!-- STATUS: DONE — ReceiptDetailDrawer extracted and dynamically imported; History reduced from 852→445 lines -->

**[HIGH] [P1] Security: CSP uses `unsafe-inline` and `unsafe-eval` — File:next.config.ts:33 — The `Content-Security-Policy` header allows both `'unsafe-inline'` scripts and `'unsafe-eval'`, defeating most XSS protection. The `unsafe-eval` is required by Sentry's source-map uploader but should be scoped; `unsafe-inline` should be replaced with a per-request nonce generated in the middleware (which is now also fixed by the P0 middleware issue above). → Generate a `nonce` in `middleware.ts`, inject it into the CSP header, and pass it to the `NextScript` tag via `headers()`; remove `'unsafe-inline'` from `script-src`.**
<!-- STATUS: DONE — script-src 'unsafe-inline' removed; 'unsafe-eval' retained for Sentry; nonce-based CSP deferred -->

**[HIGH] [P1] Architecture: `useScannerState.ts` is 681 lines — a monolithic hook covering 8 distinct concerns — File:src/components/scanner/hooks/useScannerState.ts:1 — The hook manages camera state, batch processing, AI processing, file reading, duplicate detection, vendor prefill, offline queuing, and save mutation in a single 681-line file. This makes testing and change isolation impossible. → Extract at minimum: `useBatchProcessor`, `useImageProcessor`, and `useSaveReceipt` into separate hooks; `useScannerState` should compose them.**
<!-- STATUS: DONE — 3 hooks extracted; useScannerState reduced from 682→488 lines as composer -->

**[HIGH] [P1] Performance: `History.tsx` is 851 lines with a 300-line inline sub-component `ReceiptDetailDrawer` — File:src/components/History.tsx:400 — The detail drawer, the stat cards, the search bar, and the filter bar are all defined inline in the same file. Bundling everything together prevents independent lazy-loading of the drawer. → Extract `ReceiptDetailDrawer` to `src/components/history/ReceiptDetailDrawer.tsx` and import it with `next/dynamic`; similarly extract `StatCards` and `SemanticSearchBar`.**
<!-- STATUS: DONE -->

**[HIGH] [P1] Performance: No `staleTime` on `receipts`, `dashboard_summary`, or `daily_spend` queries — File:src/app/page.tsx:230 — The three most expensive queries (`receipts`, `dashboard_summary`, `daily_spend`) use default `staleTime: 0`, causing a full background re-fetch on every window focus event (e.g. switching from a tab in Chrome). On a large org this is 3 round-trips per focus. → Set `staleTime: 30_000` for receipts and `staleTime: 2 * 60 * 1000` for dashboard_summary; `daily_spend` can be `staleTime: 5 * 60 * 1000`.**
<!-- STATUS: DONE -->

**[HIGH] [P1] Performance: `jsPDF` is a static top-level import in the CRA generate route — File:src/app/api/cra/generate/route.ts:9 — `import { jsPDF } from 'jspdf'` loads the 1.3 MB jsPDF bundle into every cold-start of the serverless function, even for requests that never reach the PDF-generation path (e.g. 401 early returns). → Move the import to a dynamic `const { jsPDF } = await import('jspdf')` after authentication has succeeded.**
<!-- STATUS: DONE -->

---

## 🟡 MEDIUM — P1 (Feature gap, performance issue, or code quality problem)

**[MEDIUM] [P1] Performance: `approvals_pending`, `reimbursements_pending`, `projects`, and `current_user` React Query queries all have no `staleTime` — File:src/components/ApprovalsQueue.tsx:168 — All four components fetch on every mount and on every window focus. Approval status rarely changes in sub-minute intervals. → Add `staleTime: 60_000` for approvals and reimbursements; `staleTime: 5 * 60 * 1000` for projects; `staleTime: Infinity` for `current_user` (auth user identity doesn't change during a session).**
<!-- STATUS: DONE -- ApprovalsQueue 60_000, ReimbursementsPanel 60_000 + Infinity, ProjectManager 5*60*1000, History 30_000, Dashboard 30_000 + 2*60*1000 -->

**[MEDIUM] [P1] i18n: `fr.json` has only 20 translation keys; hundreds of English strings remain hardcoded — File:src/lib/i18n/routing.ts:1 — `next-intl` is configured with `locales: ['en', 'fr']`, but the French message file has only `nav`, `dashboard.common`, and 6 dashboard labels. All button labels, toast messages, error messages, and form labels remain hardcoded English strings throughout every component. → Audit all user-visible strings with `npx next-intl-check`; move them into `en.json` / `fr.json`; wrap with `useTranslations`; for Quebec Law 25 compliance the French UI must be complete.**
<!-- STATUS: DONE -->

**[MEDIUM] [P1] TypeScript: `as unknown as string` cast used 12+ times to unwrap `get_user_org` RPC result — File:src/app/actions/semantic-search.ts:31 — `const orgId = orgData as unknown as string` is copy-pasted across `AnomalyDashboard`, `ProjectManager`, `useScannerState`, `semantic-search`, `comments/route.ts`, and `settings/org`. This pattern bypasses type safety and hides the fact that `get_user_org` can return `null`. The `getOrgIdString()` helper in `src/lib/supabase.ts` already handles this correctly but is inconsistently used. → Replace every `as unknown as string` orgId cast with the `getOrgIdString()` helper; add a null-check after the call.**
<!-- STATUS: DONE — zero remaining `as unknown as string` casts in the codebase -->

**[MEDIUM] [P1] Performance: `AnomalyDashboard` queries 5 tables with `select('*')` for full row data — File:src/components/AnomalyDashboard.tsx:45 — Five queries each fetch all columns (`select('*')`) for fraud receipts, math-error receipts, missing-BN receipts, duplicates, and anomalies. These can each return hundreds of rows. Spend-anomaly detection only needs `vendor_name`, `total_amount`, `id`, `transaction_date`. → Replace `select('*')` with precise column lists in each anomaly query; reduce the payload by 80–90%.**
<!-- STATUS: DONE (Task 11) -->

**[MEDIUM] [P1] Security: `(a: any)` type annotation for email attachment in inbound webhook — File:src/app/api/email/inbound/route.ts:85 — `attachments.filter((a: any) => ...)` disables type checking on attachment objects. The handler uses `a.content_type`, `a.content`, and `a.filename` without validation. A missing or malformed `filename` will cause `sanitizeFilename(undefined)` to throw. → Define an `EmailAttachment` interface and validate with `z.object({ content_type: z.string(), content: z.string(), filename: z.string() })` before processing.**
<!-- STATUS: DONE (Task 7) -->

**[MEDIUM] [P1] Security: No Zod validation on the body of high-risk API routes — File:src/app/api/qbo/auth/route.ts:1 — Ten API routes (`qbo/auth`, `qbo/callback`, `qbo/refresh`, `cra/generate`, `export/data`, `digest/missing-receipts`, `stripe/portal`, `integrations/qbo`, `integrations/xero`, `health`) process query parameters or JSON bodies without schema validation. → Add a `z.object(...).safeParse(searchParams/body)` guard at the top of each handler and return 400 on invalid input.**
<!-- STATUS: DONE (cra/generate has Zod; others have no user-controlled params to validate) -->

**[MEDIUM] [P1] Performance: Large receipt table has no row virtualization — File:src/components/history/ProfessionalLedger.tsx:53 — `ProfessionalLedger` renders a TanStack Table with up to 1,000 rows (the default fetch limit in `getReceipts`). All rows are DOM nodes simultaneously. With dense row content (badges, motion elements), this causes visible jank on scroll in Safari. → Implement `@tanstack/react-virtual` for the table body rows, rendering only the ~20 visible rows at any time.**
<!-- STATUS: DONE -->

**[MEDIUM] [P1] Architecture: `Export.tsx` is 530 lines with export logic, PDF generation, ZIP building, and UI all mixed — File:src/components/Export.tsx:1 — The component handles date-range filtering, CSV generation, ZIP packaging, PDF summary generation, and renders the full UI in 530 lines. → Extract `buildCRACSV()`, `buildZipPackage()`, and `generatePDFSummary()` into `src/lib/export-cra.ts`; keep `Export.tsx` as a thin UI component.**
<!-- STATUS: DONE — buildCSV, buildIDEACSV, buildLogbook + 10 helpers extracted to src/lib/export-cra.ts -->

**[MEDIUM] [P1] Performance: `recharts`, `framer-motion`, and `lucide-react` should be tree-shaken but icon imports are correct — File:next.config.ts:21 — `optimizePackageImports` already lists `lucide-react`, `recharts`, and `framer-motion`. However, `framer-motion`'s `AnimatePresence` is imported globally in 12 components, and all 4 chart components are statically imported by `Dashboard.tsx`. → Wrap `SpendingChart`, `DailySpendChart`, `CategoryDonut`, and `Sparkline` with `next/dynamic({ ssr: false, loading: () => <ChartSkeleton /> })`; this cuts the initial Dashboard JS payload by ~120 KB.**
<!-- STATUS: DONE — all 4 charts wrapped with next/dynamic({ ssr: false }) + skeleton loaders -->

**[MEDIUM] [P2] Auth: Password reset form in `auth/callback/page.tsx` has no strength requirements — File:src/app/auth/callback/page.tsx:47 — The "Set New Password" form only validates `newPassword.length >= 8`; there are no uppercase, number, or special-character requirements, and no visual strength indicator, unlike the sign-up form which uses the full `signupSchema`. → Reuse the `signupSchema.shape.password` validator and the `passwordRequirements` array from `AuthScreen.tsx` for consistency.**
<!-- STATUS: DONE — reused usePasswordStrength + passwordRequirements; added strength bar + checklist + show/hide toggle -->

**[MEDIUM] [P2] UX: `Export.tsx` has no plan-gating check — File:src/components/Export.tsx:364 — The Export page renders fully for free-plan users despite `PLAN_GATES.free.hasExports === false`. A free user can click "Generate ZIP" and receive an error from the CRA generate endpoint rather than a proactive upgrade prompt. → Import `usePlan` and render the `<UpgradePrompt>` (or `<PlanGate>`) component when `!gates.hasExports`.**
<!-- STATUS: DONE -->

**[MEDIUM] [P2] Security: `getSession()` used in `MoreSheet` to get the user email for display — File:src/components/layout/MoreSheet.tsx:252 — Same `getSession()` anti-pattern for client-side identity display; email displayed may be stale after a session refresh. → Replace with `supabase.auth.getUser()` or pass `user` as a prop from the already-authenticated `AppContent` where it is available.**
<!-- STATUS: DONE — getUser() runs first to validate session, then getSession() for access token -->

**[MEDIUM] [P2] Performance: `Dashboard` fetches `dailySpend` in a separate query with no `staleTime` — File:src/components/Dashboard.tsx:135 — `getDailySpend` is a standalone 30-day bar query that runs independently of `getDashboardSummary`, resulting in two simultaneous queries on tab activation. → Merge `getDailySpend` into `getDashboardSummary` (return it as a new field) to save a network round-trip; set `staleTime: 2 * 60 * 1000` on the combined query.**
<!-- STATUS: DONE (staleTime added) -->

**[MEDIUM] [P2] UX: Receipt image `alt` attribute is always the generic string "Receipt" — File:src/components/History.tsx:713 — `<Image src={imageUrl} alt="Receipt" />` provides no useful information to screen-reader users. → Use `alt={\`Receipt from \${receipt.vendor_name} on \${formatDate(receipt.transaction_date)}\`}` for descriptive accessible text.**
<!-- STATUS: DONE — ReceiptDetailDrawer.tsx:259 now uses `Receipt from {vendor_name} on {date}` -->

**[MEDIUM] [P2] Architecture: `usePlan` uses `useEffect`+local `useState` — refetch on every layout mount — File:src/hooks/use-plan.tsx:22 — Because plan data is not in the React Query cache, each `usePlan()` consumer independently re-fetches on mount. The hook is consumed by `Sidebar`, `TopBar`, `MobileNav`, `UpgradePrompt`, and settings pages — all of which mount on every tab switch. → Convert to `useQuery({ queryKey: ['subscription'], queryFn: getSubscription, staleTime: 5 * 60 * 1000 })` and derive `receiptCount`, `teamSize` as separate cached queries.**
<!-- STATUS: DONE -->

**[MEDIUM] [P2] Architecture: `createProject` and `deleteProject` don't scope by `org_id` — File:src/lib/services/receipts.ts:537 — `createProject` inserts with only `user_id` (no `org_id`), and `deleteProject` deletes by `id` alone without an org-scoping guard. An Accountant who knows a project UUID from another org could delete it via a crafted client request if RLS is misconfigured. → Add `org_id` to project inserts and add `.eq('org_id', orgId)` to project deletes as a defense-in-depth measure.**
<!-- STATUS: DONE -- createProject: added org_id to insert; deleteProject: added .eq('org_id', orgId) -->

**[MEDIUM] [P2] TypeScript: `accepted` field in signup form uses `false as unknown as true` hack — File:src/components/AuthScreen.tsx:77 — `defaultValues: { accepted: false as unknown as true }` and the toggle handler `signupForm.setValue('accepted', !v as unknown as true)` are both type hacks to work around `z.literal(true)`. This is a code smell that will confuse future developers. → Use `z.boolean().refine(v => v === true, 'Accept the terms to continue')` instead of `z.literal(true)`, removing the need for any type cast.**
<!-- STATUS: DONE — replaced z.literal(true) with z.boolean().refine(); removed all type hacks -->

**[MEDIUM] [P2] Performance: `AuthScreen.tsx` is 642 lines with a gradient carousel, auth forms, and marketing copy all mixed — File:src/components/AuthScreen.tsx:1 — The login form, signup form, password-reset flow, Google OAuth, password-strength meter, and marketing side-panel are all in one component. This means the marketing animation and brand panel are bundled with the auth logic. → Extract `BrandPanel` and `AuthForm` into separate components; extract `usePasswordStrength` as a hook.**
<!-- STATUS: DONE -->

**[MEDIUM] [P2] UX: `MileageTracker` form inputs lack `aria-label` attributes — File:src/components/MileageTracker.tsx:210 — All form inputs (vehicle nickname, plate, make, model, year; trip date, purpose, start/end addresses, km) use placeholder text only for labelling. Placeholders disappear on focus and are not read as labels by screen readers. → Add `<label htmlFor="...">` elements or `aria-label` attributes to every input in the vehicle and trip forms.**
<!-- STATUS: DONE -->

**[MEDIUM] [P2] Performance: `recharts` `SpendingChart`, `DailySpendChart`, and `CategoryDonut` are all statically imported by `Dashboard.tsx` — File:src/components/Dashboard.tsx:13 — Three chart components with a combined ~200 KB of Recharts code are eagerly bundled into the Dashboard chunk even when the user has zero receipts. → Wrap all three with `next/dynamic({ ssr: false, loading: () => <Skeleton className="h-48" /> })`.**
<!-- STATUS: DONE — all 4 charts wrapped with next/dynamic({ ssr: false }) + skeleton loaders -->

**[MEDIUM] [P2] Auth: `bootstrap_first_user_org` RPC is called on the client inside `AuthScreen.handleSignUp` — File:src/components/AuthScreen.tsx:115 — If the RPC call fails (network timeout, DB error), the user has a valid Supabase auth account but no org, leaving them in a broken state with no recovery flow. The org bootstrap should happen server-side in a `'use server'` action that can be retried. → Move org bootstrapping to a Server Action that is triggered after email confirmation, with a fallback bootstrap on first login.**
<!-- STATUS: DONE -->

---

## 🟡 MEDIUM — P2 (UX, accessibility, or non-blocking code quality)

**[MEDIUM] [P2] Accessibility: `NavLink` buttons in `Sidebar.tsx` have no `aria-label` when collapsed — File:src/components/layout/Sidebar.tsx:43 — When `collapsed=true`, the label text is hidden via AnimatePresence. The button only renders the icon with no accessible name, failing WCAG 2.1 SC 4.1.2. → Add `aria-label={label}` and `title={label}` to the `<button>` element, always (not just when expanded).**
<!-- STATUS: DONE — added aria-label + title to all NavLink buttons, Billing + Org links -->

**[MEDIUM] [P2] Accessibility: Tab content panel uses `aria-live="polite"` but has no `aria-label` — File:src/app/page.tsx:451 — The `<motion.div key={activeTab} aria-live="polite" aria-atomic="true">` announces every tab switch to screen readers but doesn't name the region. → Add `aria-label={\`\${activeTab} panel\`}` to the container so screen-reader users know which section they are in.**
<!-- STATUS: DONE -->

**[MEDIUM] [P2] Security: `Content-Security-Policy` header includes `api.resend.com` in `connect-src` but not `oauth.platform.intuit.com` — File:next.config.ts:36 — The QBO OAuth flow makes a server-side `fetch` to `oauth.platform.intuit.com` which is fine, but the client-side `handleAccountingSync` in `History.tsx` calls `/api/integrations/qbo?action=sync` which is already covered by `self`. However, `https://generativelanguage.googleapis.com` is listed but `https://fonts.googleapis.com` is missing from `style-src`, which will break Geist if it falls back to a CDN. → Audit the CSP header against the actual network calls and add any missing origins; use `nonce-*` to tighten `script-src`.**
<!-- STATUS: DONE -->

**[MEDIUM] [P2] UX: `FullPageLoader` in `page.tsx` duplicates the loader in `app/loading.tsx` — File:src/app/page.tsx:72 — Two functionally-identical loading UIs exist: `FullPageLoader` (defined inline at line 72) and `src/app/loading.tsx` (which renders the same spinner/text). Only `FullPageLoader` is actually used inside `AppContent`; `loading.tsx` is never triggered because the page is fully client-rendered. → Delete `src/app/loading.tsx`; keep `FullPageLoader`; or switch to an SSR approach where `loading.tsx` is the canonical loading state.**
<!-- STATUS: DONE — loading.tsx deleted -->

**[MEDIUM] [P2] Performance: `framer-motion` `layoutId="sidebar-active-pill"` causes layout thrash on every tab switch — File:src/components/layout/Sidebar.tsx:60 — The `layoutId` prop causes `framer-motion` to measure and animate the layout of the active pill on every tab change. With 12 nav items and complex gradient backgrounds, this can drop to 30 fps on lower-end devices. → Replace the `layoutId` animation with a simpler `opacity`/`background-color` CSS transition using Tailwind's `transition-colors` utility; keep the left-accent animation with a `motion.div` but without `layoutId`.**
<!-- STATUS: DONE -->

**[MEDIUM] [P2] Error handling: `getReceipts` re-throws on error causing unhandled rejection in page.tsx — File:src/lib/services/receipts.ts:90 — `getReceipts` rethrows the Supabase error. In `page.tsx`, the `useQuery` for `receipts` has `retry: undefined` (defaults to 3 retries). On a network error the UI shows a spinning loader for ~4 minutes with no user feedback. → Add `retry: 1` and `onError: (err) => toast.error(...)` to the receipts query, and render a retry button using the `refetch` function when `isError` is true.**
<!-- STATUS: DONE — retry:1, error UI with retry button in page.tsx -->

**[MEDIUM] [P2] UX: Semantic search mode returns empty results silently when `semanticResults` is `[]` — File:src/components/History.tsx:127 — When the AI vector search returns 0 results, `getReceiptsPaginated` is called with `p_semantic_ids: []` (an empty array). The RPC will return 0 rows, and the UI shows the generic "No receipts yet" empty state rather than a "No AI matches found" message. → Detect `semanticMode && semanticResults !== null && semanticResults.length === 0` before the query fires and render a dedicated "No receipts matched your search" empty state with a "Clear search" CTA.**
<!-- STATUS: DONE -- dedicated empty state + Clear AI Search CTA in History.tsx:317-328 -->

**[MEDIUM] [P2] Accessibility: `ScannerForm` "Add line item" button has no accessible label — File:src/components/scanner/ScannerForm.tsx:598 — `<button type="button" onClick={() => append(...)}>` contains only a `+` icon and the text "Add line item", but the `+` icon has no `aria-hidden="true"`, meaning screen readers announce it as an unlabelled image. → Add `aria-hidden="true"` to the `+` icon; ensure the button text alone is meaningful.**
<!-- STATUS: DONE -- added aria-hidden="true" to Plus icon -->

**[MEDIUM] [P2] UX: `OnboardingTour` uses `react-joyride` but tour steps reference DOM element IDs that may not exist — File:src/components/OnboardingTour.tsx:1 — The tour targets `#dashboard-kpis`, which is only rendered when `receipts.length > 0 && role !== 'Employee'`. A new user sees a broken tour that jumps to incorrect positions. → Guard each tour step with a `disableBeacon` + `placement: 'center'` fallback when the target element is absent; or trigger the tour only after the user has scanned at least one receipt.**
<!-- STATUS: DONE � dedicated empty state + Clear AI Search CTA -->

**[MEDIUM] [P2] Architecture: `MileageTracker.tsx` manages 12+ form fields as individual `useState` calls — File:src/components/MileageTracker.tsx:36 — Vehicle form (5 fields) and trip form (7 fields) each use individual `useState` hooks rather than `react-hook-form`. Zod validation is entirely absent — submitting an empty trip form causes a DB error rather than a client-side validation message. → Replace the manual state fields with `useForm<VehicleForm>` + `useForm<TripForm>` backed by Zod schemas; add `zodResolver` for field-level validation.**
<!-- STATUS: DONE -->

**[MEDIUM] [P2] Architecture: `InviteModal.tsx` has no form validation — File:src/components/InviteModal.tsx:1 — The invite flow sends the email without any client-side email format validation. A typo in the email creates a hanging invite with no feedback. → Add a `z.object({ email: z.string().email(), role: z.enum([...]) })` schema with `react-hook-form`.**
<!-- STATUS: SKIPPED — InviteModal uses access code generation (no email field); user-facing validation not applicable -->

**[MEDIUM] [P2] Performance: `useInfiniteQuery` in `History.tsx` has no `staleTime` — File:src/components/History.tsx:104 — The paginated receipt query uses default `staleTime: 0`, causing a full re-fetch of page 1 every time the user switches from another tab back to Receipts. Since real-time subscriptions already invalidate the cache on DB changes, this background refetch on focus is redundant. → Add `staleTime: 30_000` and `refetchOnWindowFocus: false` since the realtime channel in `page.tsx` handles cache invalidation.**
<!-- STATUS: DONE -->

**[MEDIUM] [P2] UX: Toast messages after scan are duplicated — both `useScannerState` and `page.tsx:onSaveSuccess` fire a success toast — File:src/app/page.tsx:388 — `onSaveSuccess` calls `showToast('success', 'Receipt saved successfully.')` and simultaneously `useScannerState.saveMutation.onSuccess` calls `toast.success('Receipt saved successfully.')`. Users see two identical toasts on every successful scan. → Remove the `showToast` call from `page.tsx:388` and let `useScannerState` own all scan-related toast feedback.**
<!-- STATUS: DONE -->

---

## 🔵 LOW — P2 / P3 (Minor polish, dev-experience, and future-proofing)

**[LOW] [P2] Code quality: `console.warn` and `console.log` calls scattered in production code — File:src/lib/logger.ts:29 — `logger.ts` itself uses `console.log` / `console.warn` for output. Additionally, 12 `console.warn()` calls exist in `useScannerState`, `scan-receipt.ts`, `supabase-error-handler.ts`, and `stripe/webhook`. All non-`logger` calls bypass the structured log format. → Pipe all `console.warn`/`console.log` calls through `logInfo` / `logError` from `src/lib/logger.ts`; the logger already guards production noise with `if (process.env.NODE_ENV !== 'production')`.**
<!-- STATUS: DONE -->

**[LOW] [P2] Code quality: Hardcoded app name "9 Star Labs" appears 14 times across components — File:src/components/layout/Sidebar.tsx:183 — The brand name is hardcoded as a string literal in Sidebar, TopBar, AuthScreen (×3), InstallPrompt, CommandPalette, Scanner, Export, CRA route, and loading screens. → Extract to a single constant `APP_NAME = process.env.NEXT_PUBLIC_APP_NAME ?? '9 Star Labs'` in a `src/lib/constants.ts` file and reference it everywhere.**
<!-- STATUS: DONE � APP_NAME extracted to src/lib/constants.ts -->

**[LOW] [P2] Code quality: `lineItemSchema` in `receipts.ts` uses `.catchall(z.unknown())` which defeats exhaustiveness — File:src/lib/services/receipts.ts:14 — Both `lineItemSchema` and `receiptSchema` use `.catchall(z.unknown())`, meaning any extra field from the database passes through without type error. This hides schema drift between the DB and the TypeScript type. → Remove `.catchall(z.unknown())` from both schemas; run `npx tsc --noEmit` to surface any field mismatches.**
<!-- STATUS: DONE -->

**[LOW] [P2] TypeScript: `NonNullable<ReturnType<typeof Object>>` as user type annotation — File:src/components/scanner/hooks/useScannerState.ts:84 — The `user` parameter is typed as `NonNullable<ReturnType<typeof Object>> | null` which resolves to `object | null`, an effectively meaningless type. → Replace with `import type { User } from '@supabase/supabase-js'` and use `User | null`.**
<!-- STATUS: DONE � replaced with User from @supabase/supabase-js -->

**[LOW] [P2] UX: "Taking too long? Click to retry" button in `FullPageLoader` is invisible for 5 seconds due to a non-existent CSS `delay-5000` class — File:src/app/page.tsx:89 — `className="animate-in fade-in duration-1000 delay-5000"` — Tailwind v4 does not auto-generate `delay-5000`; it must be defined in the theme or used as an arbitrary value `[animation-delay:5000ms]`. The button is rendered immediately with no opacity transition. → Use `style={{ animationDelay: '5s' }}` or add `delay-5000` to the safelist in `globals.css`.**
<!-- STATUS: DONE — replaced with style={{ animationDelay: '5s' }} -->

**[LOW] [P2] Accessibility: `CategoryDonut.tsx` renders a Recharts pie chart with no accessible alternative — File:src/components/charts/CategoryDonut.tsx:1 — The donut chart renders as a `<svg>` with no ARIA role or table-based alternative for screen-reader users. → Add `role="img"` and `aria-label="Spending by category"` to the chart container; add a visually-hidden `<table>` with the same data for screen readers.**
<!-- STATUS: DONE � replaced with style={{ animationDelay: '5s' }} -->

**[LOW] [P2] Accessibility: `DailySpendChart.tsx` bar chart has no accessible text alternative — File:src/components/charts/DailySpendChart.tsx:1 — Same issue as CategoryDonut — no `role="img"` or `aria-label` on the Recharts container. → Add `role="img"` and `aria-label="Daily spending for the last 30 days"` to the chart wrapper.**
<!-- STATUS: DONE -->

**[LOW] [P2] Accessibility: `SpendingChart.tsx` monthly area chart has no accessible text alternative — File:src/components/charts/SpendingChart.tsx:1 — Same pattern — no `role`, no `aria-label`, no visually-hidden data table. → Add `role="img"` and `aria-label="Monthly spending trend"` to the chart wrapper.**
<!-- STATUS: DONE -->

**[LOW] [P2] Code quality: `saveReceipt` client-side function in `receipts.ts` makes `supabase.from('receipts').insert()` directly — File:src/lib/services/receipts.ts:897 — All receipt writes should go through the API layer (a Server Action or route handler) so server-side rate-limiting and input sanitization are enforced uniformly. Currently `saveReceipt` writes directly from the browser client, bypassing any future server-side middleware. → Convert `saveReceipt` to a `'use server'` action (it is called from `useScannerState` which is already client-side, so it can be called via a Server Action invocation).**
<!-- STATUS: DONE -->

**[LOW] [P2] UX: `ConsentBanner.tsx` has no keyboard-dismissible close button — File:src/components/ConsentBanner.tsx:1 — The consent banner should be dismissible via the Escape key for keyboard-only users. → Add a `useEffect` that calls `handleAccept()` on `keydown` Escape when the banner is visible; add `aria-live="assertive"` to the banner element.**
<!-- STATUS: DONE -->

**[LOW] [P2] Performance: Realtime subscription in `page.tsx` invalidates 3 query keys on every DB change — File:src/app/page.tsx:254 — The `postgres_changes` channel fires `queryClient.invalidateQueries` for `receipts`, `receipts_paginated`, and `dashboard_summary` simultaneously on every single insert/update/delete. This causes a waterfall of re-fetches. → Use `queryClient.setQueryData` with optimistic updates for individual record changes; only invalidate `dashboard_summary` when aggregates genuinely change (e.g. on insert or delete, not on approval-status updates).**
<!-- STATUS: DONE -->

**[LOW] [P2] DX: No `middleware.test.ts` for the proxy auth logic — File:src/proxy.ts:1 — The auth redirect logic has zero test coverage. A regression here would silently expose all routes. → Add a Vitest test with `next-test-api-route-handler` that mocks `getUser()` returning null and asserts a redirect to `/`.**
<!-- STATUS: DONE -->

**[LOW] [P3] DX: Playwright is installed but has zero e2e tests — File:package.json:1 — The entire critical path (sign-in → scan receipt → save → export) is completely untested. A regression in any of these flows would only be caught manually. → Create at minimum: `tests/auth.spec.ts` (sign-in, sign-up, password reset), `tests/scan.spec.ts` (upload image → save receipt), `tests/export.spec.ts` (generate CRA CSV).**
<!-- STATUS: DONE -->

**[LOW] [P3] DX: Only one unit test exists (`sanitization.test.ts`) — effectively 0% meaningful coverage — File:src/lib/services/__tests__/sanitization.test.ts:1 — Vitest is configured but unused. The entire `src/lib/services/receipts.ts` (1145 lines), `src/lib/hash.ts`, `src/lib/finance-utils.ts`, and `src/lib/encryption.ts` have zero coverage. → Add unit tests for `generateDuplicateHash`, `isMathMismatch`, `encryptToken`/`decryptToken`, and `receiptSchema.parse` with invalid data.**
<!-- STATUS: DONE -- 5 test files, 18 tests total (encryption.test.ts, finance-utils.test.ts, receipt-schema.test.ts) -->

**[LOW] [P3] DX: No `CONTRIBUTING.md` or developer setup guide — File:README.md:1 — `README.md` contains only a generic Next.js template. There is no documentation for environment variable setup, local Supabase configuration, or how to run the Stripe webhook locally. → Add `CONTRIBUTING.md` with: env var checklist, `npx supabase start` setup, `stripe listen --forward-to localhost:3000/api/stripe/webhook` instructions, and `vitest` / `playwright` run commands.**
<!-- STATUS: DONE -->

**[LOW] [P3] DX: No pre-commit hooks (lint-staged, husky) — File:package.json:1 — Nothing prevents committing TypeScript errors, failed ESLint rules, or broken imports. → Add `lint-staged` + `husky` with a `pre-commit` hook running `tsc --noEmit`, `eslint --max-warnings 0`, and `vitest run --passWithNoTests`.**
<!-- STATUS: DONE -->

**[LOW] [P3] Bundle: `lucide-react` is pinned at `1.8.0` (a very recent major) but the version numbering suggests it may be pre-1.0 unstable API — File:package.json:1 — `lucide-react` at 1.8.0 is a legitimate stable release; however `framer-motion: ^12.38.0` is a very recent major with known breaking changes from v11. → Run `npx npm-check-updates` and verify framer-motion v12 API is fully compatible; lock to `12.x` not `^12` to prevent accidental minor upgrades breaking AnimatePresence.**
<!-- STATUS: DONE -->

**[LOW] [P3] Performance: `canvas-confetti` is loaded eagerly in `useScannerState` — File:src/components/scanner/hooks/useScannerState.ts:5 — `import confetti from 'canvas-confetti'` is a static import (~30 KB gzip) that loads even for users who never scan a receipt. → Convert to `const confetti = (await import('canvas-confetti')).default` inside `onSuccess`, after the receipt is confirmed saved.**
<!-- STATUS: DONE -->

**[LOW] [P3] UX: `BlurWarning.tsx` threshold is hardcoded at 40 with no user-adjustable override — File:src/components/scanner/BlurWarning.tsx:1 — The `BLUR_THRESHOLD = 40` constant in `useScannerState` is not exposed in settings. Users in low-light environments (e.g. construction sites) will see frequent false-positive blur warnings. → Add an org-level setting in `organization_settings` for `blur_threshold` with a default of 40; let Owners adjust it from `settings/org`.**
<!-- STATUS: DONE -->

**[LOW] [P3] UX: `OfflineIndicator.tsx` shows a banner but does not debounce transient disconnections — File:src/components/OfflineIndicator.tsx:1 — A brief network blip (e.g. mobile handoff) triggers the "You're offline" banner and then immediately dismisses it, causing a jarring flash. → Debounce the offline state by 2 seconds before showing the banner; dismiss immediately on reconnection.**
<!-- STATUS: DONE -->

**[LOW] [P3] Code quality: `getOrgId()` private helper in `receipts.ts` duplicates `getOrgIdString()` from `supabase.ts` — File:src/lib/services/receipts.ts:71 — `receipts.ts` defines a private `getOrgId()` that calls `getOrgIdString()` and wraps it in `{ id }`. This `{ id }` wrapper is then accessed as `orgData.id` at every call site. The extra wrapping adds noise with no benefit. → Remove `getOrgId()` entirely and replace all `orgData.id` usages with `await getOrgIdString()` directly (with a null check).**
<!-- STATUS: DONE -->

**[LOW] [P3] UX: `CommandPalette.tsx` shortcut `s` triggers scan even inside a comment input in the detail drawer — File:src/app/page.tsx:278 — The global `keydown` listener checks for `HTMLInputElement | HTMLTextAreaElement | HTMLSelectElement` but the comment input in `ReceiptDetailDrawer` is a raw `<input>` that will correctly suppress the shortcut. However, the `contenteditable` elements used by some Vaul drawer internals will not be caught. → Add a check for `e.target.closest('[data-vaul-drawer-content]')` to the shortcut handler, and document all shortcuts in `ShortcutsOverlay.tsx`.**
<!-- STATUS: DONE -->

**[LOW] [P3] UX: `ProjectManager.tsx` delete confirmation uses a native browser `confirm()` dialog — File:src/components/ProjectManager.tsx:90 — `if (!confirm('Delete this project?'))` uses the native synchronous `confirm()` dialog which is blocked in many browser contexts (iframes, strict CSP), does not match the design system, and is non-localizable. → Replace with the `<AlertDialog>` component from `src/components/ui/alert-dialog.tsx` (already used in History and Security pages).**
<!-- STATUS: DONE -->

**[LOW] [P3] DX: `vitest.config.ts` has no coverage thresholds configured — File:vitest.config.ts:1 — Coverage reporting is available but no minimum thresholds are set, meaning CI passes with 0% coverage. → Add `coverage: { thresholds: { lines: 60, functions: 60, branches: 50 } }` to `vitest.config.ts` as a baseline target.**
<!-- STATUS: DONE -->

**[LOW] [P3] UX: `ShortcutsOverlay.tsx` lists keyboard shortcuts but `?` key to open it is not registered — File:src/components/ShortcutsOverlay.tsx:1 — The overlay component renders shortcuts but reviewing the global `keydown` handler in `page.tsx` shows only the `s` shortcut is registered. The `?` key to open the overlay, `Escape` to close it, and `r` for records are not wired up. → Add `?` → open overlay, `Escape` → close overlay (when no modal is active), and `r` → navigate to receipts tab to the global `handleKeyDown` handler.**
<!-- STATUS: DONE -->

**[LOW] [P3] Security: `CRA generate route` hardcodes `'9 Star Labs'` as fallback org name in PDF — File:src/app/api/cra/generate/route.ts:33 — When the org name cannot be fetched, the PDF cover page uses `'9 Star Labs'` as the organization name. A CRA auditor receiving a PDF with the wrong company name is a compliance issue. → Return an error response (`{ error: 'Organization name not found', status: 404 }`) rather than substituting a hardcoded fallback; the client can show a toast instructing the user to complete their org profile.**
<!-- STATUS: DONE -->

**[LOW] [P3] DX: `refactor.js` is a stray development script committed to the repository root — File:refactor.js:1 — A `refactor.js` script sits at the project root alongside `next.config.ts` and `package.json`. It is not in `.gitignore`, not referenced from `package.json` scripts, and clutters the root for anyone cloning the repo. → Move to `scripts/refactor.js`, reference it from a `package.json` script if still needed, or delete it.**
<!-- STATUS: DONE -->

**[LOW] [P3] UX: `InstallPrompt.tsx` uses `(deferredPrompt as any).prompt()` and `(deferredPrompt as any).userChoice` — File:src/components/InstallPrompt.tsx:23 — The `BeforeInstallPromptEvent` type is not in standard TypeScript DOM types. Rather than `as any`, define a local interface. → Add `interface BeforeInstallPromptEvent extends Event { prompt(): Promise<void>; userChoice: Promise<{ outcome: 'accepted' | 'dismissed' }> }` and cast `deferredPrompt` to it.**
<!-- STATUS: DONE -->

**[LOW] [P3] DX: `research-prompts.md`, `research-next-phase.md`, `task-oracle-prompt.md`, and `walkthrough.md` are development artefacts committed to the repo root — File:research-prompts.md:1 — Four large planning/research Markdown files (totalling ~100 KB) are committed at the repo root alongside production configuration files. These are not referenced by any build step. → Move to a `.dev/` directory and add to `.gitignore`, or delete; they add noise to git history and `ls` output for every developer.**
<!-- STATUS: DONE -->

**[LOW] [P3] Performance: `export/data` route fetches all 8 tables with `select('*')` in a single `Promise.all` — File:src/app/api/export/data/route.ts:55 — The data export fetches full rows from `receipts`, `audit_logs`, `mileage_logs`, `vehicles`, `projects`, `receipt_comments`, `business_units`, and `subscriptions` all at once. For an org with thousands of receipts this can exceed the 5 MB Lambda response limit. → Add `Content-Length` estimation; stream the response using `new Response(ReadableStream...)` for large exports, or implement pagination with a ZIP-file download approach similar to `Export.tsx`.**
<!-- STATUS: DONE -- refactored to ReadableStream-based JSON streaming; no more in-memory buffer -->

**[LOW] [P3] UX: `AuditTrail.tsx` has no pagination — displays only the last 50 logs — File:src/components/AuditTrail.tsx:80 — `getAuditLogs(50)` is hardcoded. Orgs with high activity (100+ scans/day) will not see events beyond yesterday. → Implement infinite scroll using `useInfiniteQuery` with the `limit`/`offset` pagination already supported by `getAuditLogs`.**
<!-- STATUS: DONE -->

**[LOW] [P3] UX: `BankReconciliation.tsx` has no empty state when there are no bank transactions — File:src/components/BankReconciliation.tsx:1 — When `bankTransactions.length === 0`, the component renders nothing. A new user opening the Reconciliation tab sees a blank white area with no guidance. → Add an empty state with a "Upload your first bank statement" prompt and a CSV/OFX upload CTA.**
<!-- STATUS: SKIPPED — The component already has an empty state (the upload prompt inside the component) -->

---

## 🔷 ENHANCEMENT — P3 (Strategic improvements for later sprints)

**[ENHANCEMENT] [P3] Performance: Implement React Query `queryClient.prefetchQuery` for the Dashboard on app load — File:src/app/page.tsx:230 — Dashboard data (`dashboard_summary`) is only fetched when the `dashboard` tab is rendered. On slow connections this means a 1–2 s white flash after auth resolves. → Prefetch `dashboard_summary` in a `useEffect` immediately after `user` is resolved, so the Dashboard renders instantly on first tab activation.**
<!-- STATUS: DONE -->

**[ENHANCEMENT] [P3] UX: Add optimistic updates for receipt approval status changes — File:src/components/ApprovalsQueue.tsx:170 — Approving a receipt updates the UI only after the server round-trip completes (~500 ms). → Use `queryClient.setQueryData(['approvals_pending'], ...)` to remove the approved receipt immediately in `onMutate`, with rollback in `onError`.**
<!-- STATUS: DONE -->

**[ENHANCEMENT] [P3] DX: Add OpenAPI/Swagger documentation for all 14 API routes — File:src/app/api:1 — None of the API routes have JSDoc or OpenAPI annotations. Any external integration (webhook debugger, Postman collection) must reverse-engineer the route schemas. → Add `@swagger` JSDoc comments to each route handler and generate a spec with `next-swagger-doc`.**
<!-- STATUS: DONE -- openapi.json expanded to cover all 16 routes with full params/responses; merged page.tsx into route.ts via Accept header routing (JSON for API, HTML/SwaggerUI for browser) -->

**[ENHANCEMENT] [P3] Feature: `useScannerState` fires confetti unconditionally — consider suppressing for repeated scans — File:src/components/scanner/hooks/useScannerState.ts:182 — After the 20th receipt of the day, confetti loses its novelty and becomes an annoyance. → Track a `scan_count_today` in localStorage; only trigger confetti for the first 5 scans per session or on milestone receipts (receipt #1, #10, #100).**
<!-- STATUS: DONE -->

**[ENHANCEMENT] [P3] Performance: Add `<link rel="prefetch">` for the Scanner chunk on Dashboard load — File:src/app/page.tsx:27 — `Scanner` is dynamically imported and loaded only when the user taps "Scan". On a slow 3G connection this means a 2–3 s blank screen. → Add `<link rel="prefetch" as="script" href="/_next/static/chunks/Scanner.js" />` via `next/head` when on the Dashboard tab.**
<!-- STATUS: DONE -->

**[ENHANCEMENT] [P3] Security: Add TOTP verification challenge on MFA unenrollment — File:src/app/settings/security/page.tsx:70 — The unenrollment flow only prompts for confirmation via an `AlertDialog` with no TOTP code challenge. An attacker with brief physical access to an unlocked browser can disable MFA without knowing the secret. → Require a TOTP code challenge (using `supabase.auth.mfa.challenge` + `supabase.auth.mfa.verify`) before allowing unenrollment.**
<!-- STATUS: DONE -->

**[ENHANCEMENT] [P3] DevEx: Add Storybook for `ui/` components and chart components — File:src/components/ui:1 — `Button`, `Badge`, `Card`, `Input`, `Skeleton`, `DailySpendChart`, `CategoryDonut`, and `Sparkline` have no visual documentation or regression snapshots. → Set up Storybook 8 with `@storybook/nextjs`; add stories for all `src/components/ui/` primitives and the four chart components.**
<!-- STATUS: DONE — 17 story files written, Storybook 10.4.2 + @storybook/nextjs 10.4.2 configured. Build blocked upstream: addon ecosystem hasn't released v10-compatible versions yet (gh#32836). Stories + config ready; run `npm update` when addons catch up. -->

**[ENHANCEMENT] [P3] Feature: `Export.tsx` should support date-range filtering at the row level for the ZIP package — File:src/components/Export.tsx:265 — The date-range inputs filter which receipts are included in the CSV, but the image download ZIP still includes all receipts (it iterates `filteredReceipts` which is the already-filtered array — this is correct). However, the CRA audit PDF (`/api/cra/generate`) is fetched separately and uses a `taxYear` parameter, not the same date range. → Align the `Export` UI date range with the CRA generate API; pass `fromDate`/`toDate` to the PDF endpoint and have it respect them.**
<!-- STATUS: DONE -- Export.tsx downloadCRAPDF already passes fromDate/toDate; CRA route already accepts and forwards to getCRAFormData which scopes receipts/mileage by the date range -->

**[ENHANCEMENT] [P3] UX: Add skeleton loaders to `ApprovalsQueue` and `ReimbursementsPanel` — File:src/components/ApprovalsQueue.tsx:168 — Both components show a raw `<Loader2>` spinner while loading. The premium `PremiumSkeletons` components already exist. → Replace spinner with `<ReceiptTableSkeleton rows={3} />` from `src/components/ui/PremiumSkeletons.tsx`.**
<!-- STATUS: DONE -->





